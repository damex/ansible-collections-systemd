---
- name: Ensure systemd udev
  tags:
    - udev
    - systemd
    - systemd_udev
  block:
    - name: Ensure systemd udev udev.conf
      ansible.builtin.import_tasks: udev.conf.yml
    - name: Ensure systemd udev rules
      ansible.builtin.import_tasks: rules.yml
    - name: Ensure systemd udev systemd service
      ansible.builtin.import_tasks: systemd_service.yml
