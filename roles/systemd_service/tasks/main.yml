---
- name: Ensure {{ systemd_service_name }} systemd service
  tags:
    - service
    - systemd_service
    - systemd
  block:
    - name: Ensure {{ systemd_service_name }} systemd service
      ansible.builtin.import_tasks: systemd_service.yml
